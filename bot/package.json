{
  "name": "txtclan-bot",
  "version": "1.0.18",
  "private": true,
  "type": "module",
  "description": "TXTClan Bot — Tickets, Shop, Giveaways und Vouches",
  "scripts": {
    "start": "node --experimental-sqlite dist/index.js",
    "dev": "node --watch --experimental-sqlite --experimental-strip-types src/index.ts",
    "test": "node --test dist/util.test.js",
    "build": "tsc --pretty false"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "discord.js": "^14.27.0",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
